FloatLayout:
    Background:
        id: background
        canvas.before:
            Rectangle:
                size: self.size
                pos: self.pos
                source: "bg.jpg"
            Rectangle:
                size: self.width, 140
                pos: self.pos[0], self.pos[1] + self.height - 140
                texture: background.cloud_texture
